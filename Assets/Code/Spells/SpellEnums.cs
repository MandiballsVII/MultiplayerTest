public enum SpellSchool { Red, Blue, Green, Black, White }
public enum SpellType { Projectile, Area, Self, Summon } // (Self = cura/buff propio)
public enum AreaShape
{
    Circle, // centrado en caster
    Cone,   // orientado desde las manos
    Explosion, // generado por un proyectil al impactar
}

